import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ganeshweather.com',
  appName: 'weather_ganesh',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
